import React from 'react'

const StudentFilterRecord = (key) => {
  return (
      console.log(key)
    // fetch("http://localhost:8080/api/studentInfo/byFilter", {body: {"filterType": "BY_REG_DATE_ASC"}})
  )
}

export default StudentFilterRecord;