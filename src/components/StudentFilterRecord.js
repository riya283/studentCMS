import React from 'react'

const StudentFilterRecord = (key) => {
  return (
      console.log(key)
    // fetch("https://student-cms-be.herokuapp.com/api/studentInfo/byFilter", {body: {"filterType": "BY_REG_DATE_ASC"}})
  )
}

export default StudentFilterRecord;